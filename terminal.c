//basically just a pipe and run the commands given while outputting into another file w a pipe
// then just display the outpu0
//

void	draw_command_line(s_editor ed)
{
	// draw rectangle
	// draw text for command
	// when hit enter run the script 
	// pipe the output to a winow
}
